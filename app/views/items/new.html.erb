<% content_for :title, "New Item" %>

<h1>New Item for <%= @list.name %></h1>

<%= form_for [@list, @item] do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.submit "Create Item" %>
<% end %>

<%= link_to "Back to List", @list %> <!-- Link de volta para a lista -->
